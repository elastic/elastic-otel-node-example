<!DOCTYPE html>
<html>
<head>
<title>Shortlinks</title>
<link href="https://unpkg.com/sanitize.css" rel="stylesheet" />
<link href="https://unpkg.com/sanitize.css/typography.css" rel="stylesheet" />
<style>
body {
    padding: 5%;
    line-height: 1.6em;
}
label, input {
    font-size: 1em;
}
label, input[type=text], input[type=url] {
    border: 1px solid #999;
    background-color: #ffffff;
    padding: 5px;
}
label {
    border-right-width: 0px;
    background-color: #eee;
    border-radius: 4px 0 0 4px;
}
input[type=text], input[type=url] {
    border-radius: 4px;
}
#shortname {
    border-radius: 0 4px 4px 0;
}
#error {
    color: red;
}
.btn {
    background: #3498db;
    background-image: -webkit-linear-gradient(top, #3498db, #2980b9);
    background-image: -moz-linear-gradient(top, #3498db, #2980b9);
    background-image: -ms-linear-gradient(top, #3498db, #2980b9);
    background-image: -o-linear-gradient(top, #3498db, #2980b9);
    background-image: linear-gradient(to bottom, #3498db, #2980b9);
    -webkit-border-radius: 5;
    -moz-border-radius: 5;
    border-radius: 5px;
    font-family: Arial;
    color: #ffffff;
    font-size: 20px;
    padding: 10px 20px 10px 20px;
    text-decoration: none;
}
#recents {
    margin-top: 3em;
}
ul {
    padding-left: 1em;
}
li {
    list-style: none;
}
a {
    text-decoration: none;
}
a:hover {
    text-decoration: underline;
}
#recents a {
    color: black;
}
input[type="submit"] {
    margin-top: 0.6em;
    padding: 0.4em 0.8em;
    color: #ffffff;
    background-color: #3498db;
    border-radius: 5px;
    color: #ffffff;
    border-color: #3498db;
}
</style>
</head>
<body>

<form id="create-form" action="/" method="post">
    <label for="shortname">{{baseUrl}}/</label><input id="shortname" name="shortname" type="text" placeholder="shortname" value="{{shortname}}" autofocus maxlength="128" size="10"/>
    →
    <input name="url" type="url" placeholder="https://some-url" value="{{url}}" size="40"/><br/>
    <input type="submit" value="Create" class="original-button"/>
</form>
<small><a href="https://github.com/elastic/elastic-otel-node-example#readme">About</a></small>

{{#errMsg}}
<div id="error-container">
    <div id="error">{{errMsg}}</div>
</div>
{{/errMsg}}

<div id="recents">
<h2>Recent shortlinks</h2>
<ul>
{{#recentLinks}}
  <li><a href="/{{shortname}}">{{shortname}}</a></li>
{{/recentLinks}}
{{^recentLinks}}
  <li>(none)</li>
{{/recentLinks}}
</ul>
</div>

</body>
</html>
